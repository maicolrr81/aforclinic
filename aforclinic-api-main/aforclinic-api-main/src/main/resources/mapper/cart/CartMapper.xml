<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xenialsoft.api.core.cart.mapper.CartMapper">

    <resultMap id="cart" type="Cart">
        <id     property="userId"       column="user_id" />
        <id     property="productId"    column="product_id" />
        <result property="createdAt"    column="created_at" />
        <result property="createdBy"    column="created_by" />
    </resultMap>
    
    <select id="selectListById" resultMap="cart">
        select user_id
             , product_id
             , created_at
             , created_by
          from cart 
         where user_id = #{id}
    </select>
    
    <insert id="insert">
        insert into cart (
            user_id,
            product_id,
            created_by
        ) values (
            #{userId},
            #{productId},
            #{createdBy}
        )
    </insert>

    <delete id="delete">
        delete
          from cart
         where user_id = #{userId}
           and product_id = #{productId}
    </delete>

</mapper>